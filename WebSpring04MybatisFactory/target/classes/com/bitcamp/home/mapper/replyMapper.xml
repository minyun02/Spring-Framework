<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.bitcamp.home.board.ReplyDAOImp">
  	<insert id="replyInsert" parameterType="com.bitcamp.home.board.ReplyVO">
  		insert into reply(num, no, content, userid) 
  		values(memsq.nextval, #{no}, #{content}, #{userid})
  	</insert>
  	<select id="replyAllRecord" parameterType="int" resultType="com.bitcamp.home.board.ReplyVO">
  		select num, content, userid, writedate from reply where no=#{param1} order by num desc
  	</select>
  </mapper>
  